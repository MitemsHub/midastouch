# V75 CLOSEOUT — 2026-09-16 (operator decision: stop all indices activity)

**Decision:** The operator ordered a full stop of everything indices-related and a
pivot of the program to gold (XAUUSD) under the codename **MIDASTOUCH**.
Executed same day. Nothing was deleted; everything is archived and restorable.

## What was stopped

| item | action | state |
|---|---|---|
| Scheduled task `MitemshubA2FirstTradeWatch` (5-min A2 watch) | `Disable-ScheduledTask` | Disabled (task object intact) |
| Scheduled task `SyntheticIndicesPaperPipeline` (nightly paper pipeline) | `Disable-ScheduledTask` | Disabled (task object intact) |
| Terminal 49E0 (arms A2 + D) | stopped (flat-check verified first), expert blocks stripped from `Default/chart01+chart02` | Terminal relaunched EA-less; charts remain |
| Terminal 71BF (arm B, offline at stop time) | expert stripped from `Default/chart01` | Would relaunch EA-less |
| Terminal FB9A (arm C = V75MacroEngine, offline) | expert stripped from `Default/chart03` | Would relaunch EA-less |
| Python background monitors | checked `python*` processes | none running |

Every chart edit was preceded by a byte backup
`<chart>.pre_midas_20260916.bak` in the same folder — restoring an arm is a
file copy away.

## Ledger archive (checksummed)

`artifacts/archive/v75_closeout_20260916/` — 5 ledgers, `MANIFEST.json`
carries byte sizes, line counts and 16-hex sha256 per file:

| file | lines | sha256(16) |
|---|---|---|
| 49E0__…_Volatility_75_Index_A2.csv | 7 | 3827da81e2fc716a |
| 49E0__…_Volatility_75_Index_D.csv | 53 | 1e67a94547c9dd11 |
| 71BF__…_Volatility_75_Index.csv | 32 | 46b502e786858b17 |
| FB9A__…_Volatility_75_Index.csv | 43 | 393c0830d1f05742 |
| FB9A__V75MacroEngine_paper_….csv | 1 | ea789d1a57504d28 |

Flatness was verified on the running terminal's arms **before** shutdown:
A2 and D both flat (zero dangling OPEN rows), so no virtual position was
lost or orphaned by the stop.

## Standing record (unchanged by the closeout)

- **Volatility 75 (1-tick) remains the only instrument ever certified in this
  program** (fresh90 A/B positive, forward-verified by arm A). Its shipped
  preset and .ex5 are preserved in the repo and terminal trees.
- The V28/V75MacroEngine research families end with honest negative or
  break-even verdicts on real samples (registry 61 rows / 8,426 trades;
  32-cell matrix all-refused; MFE study 16,449 trades). The strategy family
  was never the failure; the cost geometry of the instruments was.
- Real account 140778269 (DerivSVG-Server-03) is untouched; balance record
  as of the 2026-09-14 go-live rehearsal: $50.22. No real order was ever
  sent by this program.

## The pivot

Successor program: **MIDASTOUCH** — gold-only engine (XAUUSD), plan approved
2026-09-16, this document is its Step 1. The research engine (sweep runner,
registry schema, build_parity, R reconciliation, floor-zone, morning status)
carries over; the market does not.
