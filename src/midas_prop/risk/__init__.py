"""Venue rules and cost arithmetic for the Upcomers $25,000 XAUUSD account.

Present as a *regular* package so no other checkout's namespace portion can shadow
it — the marker's job, and `tests/test_local_imports.py` is what checks it.
"""
